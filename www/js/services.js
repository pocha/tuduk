angular.module('starter.services', [])
.factory('wifiFactory', function ($http, $q) {
// intialize local variables.
// Add private functions.
   return {
          // Functions written here will be accessable in the controller.
          scanAndConnectToWifi: function(){
          //do all the wifireleated functionality
          }
      }
});
